$(function () {
	// code here
	//the class 'has-parallax' is made up, can be any name you want
	$('html').addClass('has-parallax');
	//simplest way to target is by using an id - #scene, .parallax is the method or function
	$('#scene').parallax();
	
});
